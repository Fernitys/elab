l = int(input('How many levels? '))
b = int(input('How big is each bush? '))

for i in range(l):
    for j in range(1,b+1,1):
        nspaces = b-j
        nstars = j+j-1
        print((' ' * nspaces) + ('*' * nstars))
